.dart_tool/
.idea/
.vscode/
build/
*.iml
.flutter-plugins
.flutter-plugins-dependencies
android/.gradle/
android/local.properties

# Địa chỉ và khóa Supabase — gắn với đúng một dự án, mỗi người một bản
.env.json

# Khóa ký APK. Mất file này là không ra được bản cập nhật cho máy đã cài.
android/key.properties
# Cấu hình Firebase cho thông báo đẩy — gắn với đúng một project, tự tải về.
android/app/google-services.json
*.jks
*.keystore

*-debug.log

node_modules/

# Bản đã điền địa chỉ thật và mã bí mật webhook của dự án mình (xem README Phần 4)
supabase/*.local.sql

__pycache__/
